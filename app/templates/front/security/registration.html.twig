{% extends 'base_back.html.twig' %}

    {% block body %}
        <h1>Inscription</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <br/>   
        <button type="submit" class="btn btn-primary">Inscription</button>
        {{ form_end(form) }}
    {% endblock %}
