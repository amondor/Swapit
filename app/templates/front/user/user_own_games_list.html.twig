{% extends 'base_front.html.twig' %}

{% block body %}
<h2>Ma Owned List</h2>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Valider</button>
{{ form_end(form) }}
{% endblock %}